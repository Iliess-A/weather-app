import { show_city } from "./show_city.js";
show_city();
//not the real min max it's the min max of the moment not the day
//so when i have time create a function that compare the max with all the element of the list
// that are on the same date, so it does'nt go to the min max of the other days.